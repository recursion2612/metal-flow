[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cdac-quantum-optimizer"
version = "0.1.0"
description = "PhysicsNeMo surrogate-assisted Qiskit Metal and AWS Palace optimizer"
requires-python = ">=3.11,<3.13"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "torch>=2.1",
    "nvidia-physicsnemo>=2.2,<3",
]

[project.optional-dependencies]
cad = [
    "quantum-metal[mesh]>=0.7.4",
]
test = ["pytest>=8"]

[project.scripts]
cdac-generate-samples = "generate_samples:main"
cdac-train-surrogate = "train_surrogate:main"
cdac-optimize = "optimize:main"

[tool.setuptools]
py-modules = ["pipeline", "optimize", "train_surrogate", "generate_samples"]

[tool.setuptools.packages.find]
include = ["src*"]
